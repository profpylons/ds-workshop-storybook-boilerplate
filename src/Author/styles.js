import styled, { css } from 'styled-components'

export const Author = styled.div`
    font-size: ${p => p.theme.sizes.font.small};
    color: ${p => p.theme.palettes.black[0]};
    padding: 0.6rem;
`;